standalone-haproxy:
  name: 'Haproxy'
  description: 'Separated Haproxy from controller'
  has_primary: true
  public_ip_required: true
  weight: 100
  conflicts:
    - controller
    - compute
  limits:
    min: 1
